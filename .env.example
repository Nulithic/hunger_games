# Optional Kokoro TTS base URL (no trailing slash).
# Dev default uses the Vite proxy → local Docker on :8880.
# VITE_KOKORO_URL=/api/kokoro
#
# Point at a remote host later (e.g. OVH VPS) if you deploy Kokoro there:
# VITE_KOKORO_URL=https://tts.example.com
